# `REPLACE`

- Deletes old row and inserts a new row.

```sql
REPLACE INTO table_name VALUES (...);
```

---
# `UPDATE`

- Modifies existing row data.

```sql
UPDATE table_name
SET column_name = value
WHERE condition;
```

---
