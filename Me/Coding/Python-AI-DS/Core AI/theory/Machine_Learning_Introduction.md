# Machine Learning Introduction

- ML = systems that learn patterns from data to make predictions or decisions — no explicit rules coded by hand
- Core idea: optimize a model (or use data directly) to generalize from past observations to unseen inputs
- Contrast with traditional programming: rules are _inferred_, not _written_
- Key requirement: the pattern must actually exist in the data — ML can't learn what isn't there

---
