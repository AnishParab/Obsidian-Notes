# PyTorch Introduction

**PyTorch** is an open-source deep learning framework built on the Torch library, primarily used for tensor computation and automatic differentiation.

It provides:
- A **tensor computation engine**
- An **automatic differentiation system**
- A flexible interface for building and training neural networks

---
