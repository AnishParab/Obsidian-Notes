# Relational DBMS (RDBMS)

![[rdbms.excalidraw|400]]

> A **Relational DBMS (RDBMS)** is a DBMS that implements the relational model — data stored in **tables (relations)**

> It it the software implementation of a **relational model**.

---
