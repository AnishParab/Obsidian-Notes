# `CLAUDE.local.md`

- **Local (Personal, Gitignored)** -> `./CLAUDE.local.md`
	- Create in your project root.
	- Claude reads it alongside the main `CLAUDE.md`, and it's automatically `gitignored` so your personal tweaks never land in the repo.

---
