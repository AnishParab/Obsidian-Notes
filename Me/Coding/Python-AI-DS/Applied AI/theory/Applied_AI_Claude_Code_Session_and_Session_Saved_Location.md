# Claude Code Sessions

- A session is one conversation with claude code, everything from when you run claude to when you type `/exit`.
- It has unique ID, captures the full message history, all file reads, all tool results, and the current working directory.

---
# Where Claude Sessions are Saved

> Sessions are saved automatically to `~/.claude/projects/` and can be resumed at any time, even after closing the terminal.

---
