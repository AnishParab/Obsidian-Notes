# Checkout Command
``` bash
git checkout nav-bar
```

Now:
``` bash
git branch
```

- This shows `nav-bar` branch.
- This means **HEAD** now points to the `nav-bar` branch as it can be seen in the **HEAD** file.

---
# Create and move to new branch
``` bash
git checkout -b nav-bar
```

---
