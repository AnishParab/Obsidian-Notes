# Pretty-Print `JSON` Responses

``` bash
curl http://localhost:3000/users | python -m json.tool
```

---
# Using `jq`

**Install**
``` bash
sudo pacman -S jq
```

**command**
``` bash
curl http://localhost:3000/users | jq
```

---
