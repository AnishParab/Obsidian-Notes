# Insert `x` Copies of a Character

Use **count + insert/append command**.

---
# Example

- `3a!` → append `!!!` after the cursor (then Vim stays in Insert mode)

**Note**
- You must press `Esc` to return to Normal mode.

---
