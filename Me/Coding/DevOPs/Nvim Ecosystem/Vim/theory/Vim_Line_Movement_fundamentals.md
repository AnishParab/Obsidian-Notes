# Basic movement keys

- `h` → left
- `j` → down
- `k` → up
- `l` → right

---
# Why these keys

- They are on the **home row** (right hand), so movement is fast without leaving the typing position.
- Using arrow keys is slower because it forces hand movement away from home row.

---
