# Use line number mode

Show line numbers for navigation:
```vim
:set number
```

or relative numbers **(Prefferred by me)**:
```vim
:set relativenumber
```

to switch off:
``` vim
:set nonumber
```
- Use `CTRL G` to know line numbers for this.

---
