# Node.js — Arch Linux Install

### Using `pacman` (system package)

```bash
sudo pacman -Syu
sudo pacman -S nodejs npm
```

Verify:
```bash
node -v
npm -v
```

**Properties**
- Managed by system package manager.
- Stable and simple.
- Version tied to Arch repositories.

---
