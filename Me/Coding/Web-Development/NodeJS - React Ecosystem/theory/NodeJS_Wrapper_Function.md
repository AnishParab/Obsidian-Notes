# Node.js Wrapper Function

``` js 
(function (exports, require, module, __filename, __dirname) {
    // Your module code actually lives in here
});
```

- Every `node.js` code is actually inside this function.

---
# NOTE

> Hence `require` works in `node.js` and does not work in `vanilla - js`.

---
