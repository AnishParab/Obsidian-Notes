# DX

- Deterministic: same state → same UI
- Mental model: focus on current state, not transitions
- Tooling: React DevTools + Error Boundaries

---
# JSX

- Syntactic sugar — colocates UI logic with structure
- Compiles to `React.createElement('h1', null, 'Hi')`
- vs HTML: string templates + separate JS files; JSX: JS functions + nested logic

---
