# Why use React ?

- Declarative UI — describe what, not how
- Virtual DOM: batches state updates, diffs and patches only changed nodes
- Portable mental model: `react-dom` (web), React Native (mobile), Electron (desktop)
- Unopinionated: routing, state, data fetching are all opt-in and replaceable
- Mature, Meta-backed, stable long-term API

---
