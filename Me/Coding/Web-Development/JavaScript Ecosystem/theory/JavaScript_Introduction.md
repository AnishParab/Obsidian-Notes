# JavaScript Introduction

- High-level, interpreted, single-threaded *scripting* language
- Originally browser-only; now runs server-side via Node.js
- Dynamically typed, prototype-based
- Event-driven, non-blocking via event loop
- Core of the web: HTML = structure, CSS = style, JS = behavior

---
