---

---
# React Fundamentals

- JavaScript library for building UIs
- Handles only the View layer — not a framework
- Declarative: describe _what_ the UI should look like, not _how_ to update it
- Component-based architecture
- Uses Virtual DOM for efficient updates
    - Virtual DOM = in-memory representation of the UI
    - Diffing algorithm determines minimal real DOM changes needed

---
