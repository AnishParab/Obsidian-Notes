# DataType Gotchas

**Gotchas**
- `typeof null === "object"` — historical bug
- `typeof NaN === "number"` — NaN is still a number type
- `undefined` vs `null`: unintentional vs intentional absence

---
