# What is LLM ?
- A **large language model** is a **neural network**, typically based on the **Transformer architecture**, trained on massive amounts of text data to learn statistical patterns in language. It predicts the **next token** (word or subword) given a sequence of previous tokens.

---
