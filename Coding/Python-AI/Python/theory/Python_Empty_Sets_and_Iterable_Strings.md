# `spices = {}`
- This is not an empty set, but rather an **empty dictionary**.

---
# Use `set()` for Empty Sets
``` python
# Empty set
spices = set()

# Add elements
spices.add("ginger")
```

---
# Make Strings Iterable of Characters
``` python
set("chai")
# {'c', 'h', 'a', 'i'}
```

---
