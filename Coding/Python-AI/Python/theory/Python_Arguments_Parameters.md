# Parameters v/s Arguments
- **Parameters** — Variables defined in a function definition that receive values when the function is called.
- **Arguments** — Actual values passed to a function when it is called.

---
# Code
``` python
# Parameters 
def prepare_chai(order):
	print("Preparing ", order)

# Arguments
prepare_chai(chai)
```

---
