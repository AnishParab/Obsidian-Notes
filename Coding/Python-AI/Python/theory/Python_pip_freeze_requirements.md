# `pip freeze > requirements.txt`

- saves exact installed package versions

Command:
```bash
pip freeze > requirements.txt
````

Install:
```bash
pip install -r requirements.txt
```

Notes:
- includes direct + transitive deps
- use inside a venv
- can get noisy for large projects

---
