# Type Checking
- `typeof` operator : 
``` js
typeof 42;        // "number"
typeof null;      // "object"  <-- JS quirk
typeof undefined; // "undefined"
```

- `instanceof` for objects :
``` js
[] instanceof Array; // true

```

- `Array.isArray([])` for arrays specifically.

---
