# **Collections & Automation**
- Run entire collections with **Collection Runner**.
- Can chain requests (use response data as input for next request).
- Good for simulating workflows (e.g. signup → login → get profile).

---
