# **Authorization**
- Postman supports:
    - **API Key**
    - **Bearer Token** (common with JWTs)
    - **Basic Auth** (username/password)
    - **OAuth 2.0** flows (for advanced APIs)

Set globally in collection/environment to avoid repetition.

---
