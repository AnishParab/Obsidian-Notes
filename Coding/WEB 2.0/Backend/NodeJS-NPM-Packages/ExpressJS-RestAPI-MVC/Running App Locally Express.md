# Command
``` bash
node app.js

```
- Then, load `http://localhost:3000/` in a browser to see the output.

---
# `OR` Automatically restart your Server
`package.json`
``` json
"scripts": {
	"start": "node --watch index.js"
},
```

``` bash
npm start

```

---
# `OR` Best Practice
[[Install Nodemon]]

---
