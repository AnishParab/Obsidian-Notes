# `package.json`
``` json
"dependencies": {
	"express": "~4.21.1"
	"@types/node": "^22.9.0"
}
```

---
> The `~` sign automatically updates the patch version.

> The `^` sign automatically updates the patch and the minor version.

---
