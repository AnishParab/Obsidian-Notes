# `fs` Module
- The `fs` module enables you to interact with the **file system** — create, read, update, delete files & directories.
- You can use it with **callbacks**, **synchronous methods**, and **promises** (`fs.promises`).

---
# By using callback and sync API'

---
