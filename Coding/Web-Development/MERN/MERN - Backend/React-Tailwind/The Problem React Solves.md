# The Problem React Solves
- You had to manually update the **DOM** (HTML Elements) using plain JavaScript or **jQuery**.
- This was **slow**, **error-prone**, and hard to maintain as apps grew larger.

---
