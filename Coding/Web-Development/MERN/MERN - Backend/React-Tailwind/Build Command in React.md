# `vite build` / `npm run build` command
- Generates a HTML, CSS and JavaScript version of your code.
- `dist` directory is generated.

---
# Note
- You have to delete the `dist` folder.
- You should only generate it at the time you want to use it.

---
