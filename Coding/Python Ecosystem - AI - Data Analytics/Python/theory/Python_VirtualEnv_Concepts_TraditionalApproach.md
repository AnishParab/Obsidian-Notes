# Environment Isolation (Python venv)
- A **virtual environment** is a self-contained Python setup per project
- Prevents **dependency collisions** between projects
- Each venv includes:
    - Python interpreter (copy/symlink)
    - Private `site-packages`
    - Config file (`pyvenv.cfg`)
- Ensures **reproducible, deterministic dependencies**

---
