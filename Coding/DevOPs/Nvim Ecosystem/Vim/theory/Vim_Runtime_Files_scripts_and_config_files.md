# Loaded Scripts (runtime files)

### List all sourced scripts
```vim
:scriptnames
```

**Purpose**
- Shows every script/config file Vim has loaded (in order).
- Useful for debugging:
    - which plugin file ran
    - which config file set an option/keymap
    - runtimepath issues
	
---
