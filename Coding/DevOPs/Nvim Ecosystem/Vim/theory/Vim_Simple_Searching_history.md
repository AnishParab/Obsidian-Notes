# Search history (`/` and `?`)

- Type `/` (don’t press Enter)
- `<Up>` → previous searches
- `<Down>` → next searches
- `<Enter>` → run selected search

**Prefix recall**
- Type `/` + first letters, then `<Up>`
- Example: `/o<Up>` → recalls `/one`

---
