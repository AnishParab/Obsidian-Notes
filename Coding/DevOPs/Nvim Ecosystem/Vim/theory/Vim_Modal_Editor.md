# Modal Editor (Vim)

### Definition
Vim is a **modal editor**: the same key press does different actions depending on the current **mode**.

### Core modes
- **Normal mode** → navigation + commands (default mode)
- **Insert mode** → insert text
- **Visual mode** → select text
- **Command-line mode** (Ex mode) → run `:` commands

---
