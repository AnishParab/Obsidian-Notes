# Open a File in Vim

```bash
vim file.txt
```

- Opens `file.txt` in Vim.
- Editing happens inside the terminal window.

---
# Empty Lines Indicator

> `~` indicates lines that do not exist in the file (past end-of-file).

---
