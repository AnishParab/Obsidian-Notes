# Git Add
``` bash
git add <file_name>
```

---
# Git Add all files
``` bash
git add .
```

---
`git add` stages changes by moving selected files from the **working directory** to the **staging area**, preparing them for the next commit in **Git**.

---
