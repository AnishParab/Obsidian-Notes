# Git Log
- `git log` displays the **commit history** of a repository, showing commit IDs, authors, dates, and messages in **Git**.

---
# Git Log Command
``` bash
git log
```

**OR**

``` bash
git log --oneline
```

---
