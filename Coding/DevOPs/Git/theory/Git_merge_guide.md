# Git merge guide
``` bash
git checkout master
```

``` bash
git branch
```

``` bash
git merge nav-bar
```
- This opens up **vim** for a message.

``` bash
git log
```

![[git_merge.excalidraw|500]]

---
# Notes
- `+++++`
This tells there was only insertion code and no deletion code was there.

- Git tries it's best to resolve **conflicts**.

- **Delete** branches which are no longer needed.

---
