# Git commit
``` bash
git commit -m "your message"
```

---
`git commit` saves the staged changes as a **snapshot** in the repository history with a message, creating a new commit in **Git**.

---
# NOTE
Simply doing:
``` bash
git commit
```
will open up **Vim**.

---
