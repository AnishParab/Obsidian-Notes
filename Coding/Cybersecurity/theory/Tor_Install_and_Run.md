# Install
- Install `tor` using Package Manager.
- Install `torbrowser-launcher` using Package Manager.
- Install `fireajail` using Package Manager.

---
# Run
``` bash
firejail torbrowser-launcher
```

---
