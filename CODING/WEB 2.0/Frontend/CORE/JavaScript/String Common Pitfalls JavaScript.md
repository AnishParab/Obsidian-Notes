# Common Pitfalls
- Using `new String()` — creates object, not primitive  
- `.length` mismatch for emojis or special symbols  
- `replace()` without regex only replaces **first occurrence**  
- Use `replaceAll()` or regex with `/g` flag

---
