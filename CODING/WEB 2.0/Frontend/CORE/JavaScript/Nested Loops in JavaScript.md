# Nested Loops
- A loop inside another loop.
- Useful for working with **2D arrays** or grids.
``` js
for (let i = 0; i < 3; i++) {
  for (let j = 0; j < 2; j++) {
    console.log(`i=${i}, j=${j}`);
  }
}
```

---
