- NextJS provides built-in support for typescript.

---
# Steps
- Rename files to `ts` or `tsx`.
- Run `next dev` ---> Automatically installs the necessary dependencies and adds a `tsconfig.json`

---
