# Syntax
``` python
if condition:
    # do something
elif another_condition:
    # do something else
else:
    # fallback
```

---
# Truthy and Falsy in If-Else
``` python
logs = []

if logs:
    print("We have logs.")
else:
    print("No logs found.")   # logs is falsy (empty list)
```

---
